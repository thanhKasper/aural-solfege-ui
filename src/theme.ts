import { createTheme } from "@mui/material";

const theme = createTheme({
  colorSchemes: {
    light: {
      palette: {
        primary: {
          main: "#748DAE",
        },
        secondary: {
          main: "#9ECAD6",
        },
        
      },
    },
  },
});

export default theme;
